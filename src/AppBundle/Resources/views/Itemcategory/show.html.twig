{% extends 'base.html.twig' %}

{% block body %}
    <h1>Itemcategory</h1>

    <table>
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ itemCategory.name }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ itemCategory.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('get_itemcategories') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
